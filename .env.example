# Non-secret variable names and safe local defaults only.
# Production values belong in the approved secret store and encrypted env lifecycle.
CANONICAL_WEBHOOK_SECRET=
CANONICAL_LOG_FORMAT=text
RUST_LOG=info

# Runtime probes are opt-in and should be set only inside an authorized sandbox.
CANONICAL_PROBE_MODULE=
CANONICAL_PROBE_MAX_BINDINGS=5000
